<div class="container">
	<table class="table table-hover">
		<thead>
			<tr>
				<th>ID</th>
				<th class="text-center">Número de productos</th>
				<th class="text-center">Total</th>
				<th class="text-center">Estado</th>
				<th class="text-center">Acciones</th>
			</tr>
		</thead>
		<tbody>
			<%@carts.each do |cart|%>
				<tr>
					<th><%= cart.id %></th>
					<td class="text-center"><%= cart.in_shopping_carts.length %></td>
					<td class="text-center"><%= cart.total%></td>
					<td class="text-center"><%= cart.status_cart.html_safe %></td>
					<td class="text-center"><%= link_to "Eliminar", cart, method: :delete%></td>
				</tr>
			<%end%>
		</tbody>
	</table>
</div>